#include "main.h"
#include <string.h>

/**
 * _strcmp - A function that compares two strings
 * @s1: input one
 * @s2: input two
 * Return: Integer
 */
int _strcmp(char *s1, char *s2)
{
int result;
result = strcmp(s1, s2);
return (result);
}