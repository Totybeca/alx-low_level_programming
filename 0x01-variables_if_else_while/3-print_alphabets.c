#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char abc;
for (abc = 'a'; abc <= 'z'; abc++)
{
Putchar(abc);
}
for (abc = 'A'; abc <= 'Z'; abc++)
}
putchar('\n');
return (0);
}
