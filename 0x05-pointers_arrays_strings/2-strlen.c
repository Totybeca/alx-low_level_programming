#include "main.h"

/**
 * _strlen - This is a function that returns the length of a string
 * @s: is a character input
 * Return: The length of @s
 */
int _strlen(char *s)
{
size_t lenght =0;

while (*s++)
length++;
return (length);
}
